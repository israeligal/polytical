import { DuelArenaSkeleton } from "@/components/skeletons/duel-arena-skeleton";

/** Route loading state — the named skeleton shares the arena's shell classes. */
export default function DuelLoading() {
  return <DuelArenaSkeleton />;
}
