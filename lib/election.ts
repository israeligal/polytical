/**
 * Officially scheduled date of the next Knesset general election, as a
 * `YYYY-MM-DD` Asia/Jerusalem wall-clock date. Used only as a one-click "close
 * date" convenience in the suggestion wizard — NOT a displayed fact attributed
 * to anyone, so it isn't bound by the cited-source rule, but it must still be a
 * real date (no guessing). `null` hides the "יום הבחירות" quick-pick entirely.
 *
 * Set this to the confirmed date when one is fixed.
 */
export const ELECTION_DATE: string | null = null;
