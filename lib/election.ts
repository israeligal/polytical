/**
 * Officially scheduled date of the next Knesset general election (the 26th
 * Knesset), as a `YYYY-MM-DD` Asia/Jerusalem wall-clock date. Used only as a
 * one-click "close date" convenience in the suggestion wizard — NOT a displayed
 * fact attributed to anyone, but it must still be a real date (no guessing).
 * `null` hides the "יום הבחירות" quick-pick entirely.
 *
 * Source (official): https://www.gov.il/he/pages/knesset26-date — 27.10.2026.
 */
export const ELECTION_DATE: string | null = "2026-10-27";
